# Copy to `.env.integration` (gitignored) and fill in real values, then:
#   source .env.integration && dart test --tag integration
#
# Read-only credentials are sufficient — the integration suite never mutates.
export OCTOPUS_ENERGY_API_KEY=
export OCTOPUS_ENERGY_ACCOUNT_NUMBER=
